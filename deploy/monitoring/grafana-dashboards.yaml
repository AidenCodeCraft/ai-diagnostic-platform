# Grafana Dashboard 自动加载配置
apiVersion: 1

providers:
  - name: "AI Diagnostic Platform"
    orgId: 1
    folder: "AI Diagnostic"
    type: file
    disableDeletion: true
    editable: false
    options:
      path: /etc/grafana/provisioning/dashboards
